# 🚀 快速开始 - 提取小红书笔记完整内容

## 📊 当前状态
- 总笔记数: 77 篇
- 已提取内容: 17 篇 (22%)
- 待提取: 60 篇

## 🎯 分步操作指南

### 第一步:提取第一批 (0-9)

1. **打开小红书网页**
   - 访问: https://www.xiaohongshu.com
   - 确保已登录你的账号

2. **打开浏览器控制台**
   - 按 `F12` 键
   - 切换到 "Console(控制台)" 标签页

3. **复制并运行脚本**
   - 复制上面输出的完整脚本(从 `// 小红书笔记完整内容提取脚本` 开始)
   - 粘贴到控制台
   - 按 `Enter` 运行

4. **等待提取完成**
   - 脚本会自动处理 10 篇笔记
   - 每篇笔记间隔 2-3 秒
   - 大约需要 30-40 秒

5. **保存结果**
   - 复制输出的 JSON 数据
   - 在项目目录创建文件: `data/extracted-batch-0.json`
   - 将数据粘贴并保存

### 第二步:提取后续批次

提取完第一批后,继续提取其他批次:

```bash
# 第2批 (10-19)
npm run extract:help -- --batch 10 20

# 第3批 (20-29)
npm run extract:help -- --batch 20 30

# 第4批 (30-39)
npm run extract:help -- --batch 30 40

# 第5批 (40-49)
npm run extract:help -- --batch 40 50

# 第6批 (50-59)
npm run extract:help -- --batch 50 60

# 第7批 (60-69)
npm run extract:help -- --batch 60 70

# 第8批 (70-76)
npm run extract:help -- --batch 70 77
```

每个批次重复相同的步骤:
1. 复制生成的脚本
2. 在浏览器控制台运行
3. 保存为对应的 batch 文件

### 第三步:合并所有批次

所有批次提取完成后:

```bash
npm run extract:merge
```

这会:
- 合并所有 `extracted-batch-*.json` 文件
- 备份原始 `notes.json`
- 更新 `notes.json` 为提取了完整内容的版本

### 第四步:生成完整文档

```bash
npm run doc:full
```

会在 `docs/` 目录生成 3 个文档:
1. `我的小红书笔记全集-完整内容.md` - 完整内容,按时间排序
2. `我的小红书笔记全集-主题分类.md` - 按标签分类
3. `我的小红书笔记全集-可打印版.txt` - 纯文本版

## 📋 快捷命令

```bash
# 查看提取状态
npm run extract:status

# 生成批次脚本 (指定起止索引)
npm run extract:help -- --batch 0 10

# 合并所有批次
npm run extract:merge

# 生成完整文档
npm run doc:full

# 启动Web服务器查看
npm run serve
```

## ⚠️ 常见问题

### Q: 为什么不能自动提取?
A: 小红书有反爬虫机制,自动提取会被重定向到登录页。使用已登录的浏览器可以绕过这个限制。

### Q: 提取的内容不完整怎么办?
A: 部分笔记可能因为页面结构不同无法提取。这种情况下保留原有的 description 字段。

### Q: 可以一次提取更多吗?
A: 可以,但建议每批不超过 20 篇,避免浏览器卡顿或被限制。

### Q: 提取失败怎么办?
A:
- 刷新小红书页面重新登录
- 减少批次数量(比如每次 5 篇)
- 检查网络连接

## 💡 提示

1. **批量操作**: 可以在多个浏览器标签页同时运行不同批次
2. **时间安排**: 每批约需 30-40 秒,全部 8 批约需 5-10 分钟
3. **数据备份**: 合并前会自动备份原始文件到 `notes.json.backup`

## 📊 提取进度追踪

运行以下命令随时查看进度:

```bash
npm run extract:status
```

会显示:
- 总笔记数
- 已提取数量和百分比
- 下一步操作建议
