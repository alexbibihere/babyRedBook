# 📝 小红书笔记完整内容提取指南

由于小红书的反爬虫机制,我们需要使用浏览器控制台脚本来提取笔记的完整正文内容。

## 🎯 使用步骤

### 步骤 1: 准备笔记数据

在项目目录运行以下命令,获取前10篇笔记的数据:

```bash
node -e "const fs=require('fs'); copy(JSON.stringify(JSON.parse(fs.readFileSync('data/notes.json')).slice(0,10)))"
```

这会将前10篇笔记的JSON数据复制到剪贴板。

### 步骤 2: 打开小红书网页

1. 在浏览器中访问: https://www.xiaohongshu.com
2. 确保已经登录你的账号
3. 按 `F12` 打开浏览器开发者工具
4. 切换到 "Console(控制台)" 标签页

### 步骤 3: 运行提取脚本

1. 打开项目中的 `browser-extract-script.js` 文件
2. 将剪贴板中的笔记数据粘贴到脚本的 `notesInput` 变量中
3. 复制整个脚本内容
4. 粘贴到浏览器控制台
5. 按 `Enter` 运行

### 步骤 4: 等待提取完成

脚本会:
- 自动访问每篇笔记页面
- 提取完整正文内容
- 提取图片链接
- 显示提取进度

提取完成后会输出JSON数据。

### 步骤 5: 保存提取的数据

1. 复制控制台输出的JSON数据
2. 创建新文件: `data/notes-extracted.json`
3. 将JSON数据粘贴并保存

### 步骤 6: 替换原始数据

在项目目录运行:

```bash
# Windows
copy data\notes-extracted.json data\notes.json

# Mac/Linux
cp data/notes-extracted.json data/notes.json
```

### 步骤 7: 生成完整文档

```bash
npm run doc:full
```

这会在 `docs/` 目录生成3个文档:
- `我的小红书笔记全集-完整内容.md` - 按时间顺序的完整内容
- `我的小红书笔记全集-主题分类.md` - 按标签分类的完整内容
- `我的小红书笔记全集-可打印版.txt` - 纯文本可打印版本

## 🔄 批量处理所有笔记

重复以上步骤,每次处理10-20篇笔记,直到所有77篇都提取完成。

第1批: `slice(0,10)` - 索引 0-9
第2批: `slice(10,20)` - 索引 10-19
第3批: `slice(20,30)` - 索引 20-29
...
第8批: `slice(70,77)` - 索引 70-76

## ⚠️ 注意事项

1. **登录状态**: 确保在小红书网页已登录,否则可能无法查看完整内容
2. **提取速度**: 脚本已设置延迟,避免请求过快被限制
3. **内容质量**: 部分笔记可能因为页面结构不同无法提取完整内容
4. **数据备份**: 提取前建议备份原始 `data/notes.json` 文件

## 💡 提示

如果提取过程中出现问题:
- 刷新页面重新登录
- 减少批量处理的数量(比如每次5篇)
- 检查网络连接是否正常

## 📊 查看提取结果

提取完成后,可以通过以下方式查看:

```bash
# 启动Web服务器
npm run serve

# 在浏览器访问
http://localhost:8080
```

网页会显示所有笔记及其完整内容。
